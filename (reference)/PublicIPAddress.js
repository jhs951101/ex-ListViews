//import publicIP from 'react-native-public-ip';

    /*
    const publicIPAddress = () => {  // 호출 방법: onPress={() => publicIPAddress()}
        publicIP()
            .then(ip => {
                console.log(ip);
            })
            .catch(error => {
                console.log(error);
                return '';
            });
    }
    */